# Copier ce fichier en .env et remplir les valeurs
# La clé API est aussi configurable directement dans l'interface (stockée en localStorage)

# Anthropic (recommandé — utilisé par défaut)
VITE_ANTHROPIC_API_KEY=sk-ant-...

# OpenAI (alternatif)
# VITE_OPENAI_API_KEY=sk-...
